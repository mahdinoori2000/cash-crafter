<div class="d-flex justify-content-between align-items-center" style="margin-bottom: 10px; padding: 0px 20px;">
  <%= button_to "Back", categories_path, method: :get, class: "btn btn-primary" %>
  <h3 class="transaction_name"><%= @category.name %> Transactions</h3>
  <strong>$<%= @payments.sum(:amount) %></strong>
</div>
